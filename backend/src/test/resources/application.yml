spring:
  datasource:
    driver-class-name: org.h2.Driver
    url: jdbc:h2:mem:recruitment_test;MODE=MySQL;DATABASE_TO_LOWER=TRUE;DB_CLOSE_DELAY=-1
    username: sa
    password:
  sql:
    init:
      mode: always
      schema-locations: classpath:db/schema-h2.sql
  jackson:
    time-zone: Asia/Shanghai

jwt:
  secret: RecruitmentSystemJwtSecretKeyForGraduationProject2026
  expiration-ms: 86400000
